# Launch House — Get Your First 100 Customers

Course delivery page with 7 sections.

Before deployment, replace the `LINKS` object near the bottom of `index.html` with the actual video, worksheet, tracker, script, framework and roadmap URLs.

The page is static and can be deployed directly to Vercel. Section completion is saved in the visitor's browser using localStorage.
